<%= form_for(@swag) do |f| %>
  <% if @swag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@swag.errors.count, "error") %> prohibited this swag from being saved:</h2>

      <ul>
      <% @swag.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
